Operadores numéricos
Ordem de procedência 

1 ()
2 **
3 * / // %
4 + - 
--------------------------------------------------------------------------------------------------------------------------
formula para calcular percentagem 
10 * 5 / 100 = 0.5
1500 * 10 / 100 = 150.0
875 * 15 / 100 = 131.25

----------------------------------------------------------------------------------------------------------------------------
validação de condição verdadeira 
sexo = str(input('Informe seu sexo: [M/F] ')).strip().upper()[0]
while sexo not in 'MmFf':
    sexo = str(input('Dados inválidos. Por favor, informe o seu sexo: ')).strip().upper()[0]
print('Sexo {} registado com SUCESSO'.format(sexo))
-----------------------------------------------------------------------------------------------------------------------------
Lista: Coleção ordenada, mutável e que permite valores duplicados.
Tuplas: Coleção ordenada, imutável e que permite valores duplicados.
Dicionários: Coleção não ordenada, mutável e que não permite valores duplicados.
Sets: Coleção não ordenada, imutável e que não permite valores duplicados.
-----------------------------------------------------------------------------------------------------------------------------

for total in galeria:
    if total[1] >= 21:
        print(f'{total[0]} é maior de idade')
        totalmaior += 1
    else:
        print(f'{total[0]} é menor de idade')
        totalmenor += 1
print(f'Temos {totalmaior} maiores e {totalmenor} menores de idade.')

-----------------------------------------------------------------------------------------------------------------------------
maior e menor
    if len(principal) == 0:
        menor = menor = temporario[1]
    else:
        if temporario[1] >= maior:
            maior = temporario[1]
        if temporario[1] <= menor:
            menor = temporario[1]
------------------------------------------------------------------------------------------------------------------------------
testes
print("calcule a area de um rectangulo")
base = input("qual o tamanho da base do seu rectangulo? ")
altura = input("qual o tamanho da altura do seu rectangulo? ")
area = float(base) * float(altura)
print(f"o seu rectangulo possui area: {area} unidade de medida")

------------------------------------------------------------------------------------------------------------------------------
inserção de dados
valores = []
while True:
    valores.append(int(input('Digite um valor: ')))
    resposta = str(input('Quer continuar [S/N] '))
    if resposta in 'Nn':
        break
print('-=' * 30)
-------------------------------------------------------------------------------------------------------------------------------
resposta
if resposta in 'Nn'
   break

-------------------------------------------------------------------------------------------------------------------------------
contagem decrescente de um número 
def reduzirNumero(numeroInt):
    while numeroInt > 0:
        print(numeroInt)
        numeroInt -= 1

reduzirNumero(5)
--------------------------------------------------------------------------------------------------------------------------------
"""
Objectos Dunder
Dunder - Duplo underscore - Duplo Underline
Atributos Mágicos - Váriaveis
Métodos Mágicos - Funções

__init__
__name__
__main__
__file__
__doc__

"""
verifica se um ficheiro existe.
print(os.path.exists('ex100.py'))

verifica se um directório existe
print(os.path.exists('pacotes'))

criar um directório
os.mkdir('python_praticas')

criar um ficheiro
os.mknod('olamundo.py')
-------------------------------------------------------------------------------------------------------------------------------

Trabalhar com cores.

# ex: \033[0;33;44m
# Style-> 0 = None; 1 = Bold; 4 = Underline; 7 = Negative.
# Texto -> 30 = branco; 31 = vermelho; 32 = verde; 33 = amarelo; 34 = azul;
# 35 = lilás; 36 = verde ciano; 37 = ciano.
# Back -> 40 = branco; 41 = vermelho; 42 = verde; 43 = amarelo; 44 = azul;
# 45 = lilás; 46 = verde ciano; 47 = ciano.

print('\033[0;36mOi bom dia')
print('\033[0;30;41mOi bom dia')
print('\033[4;33;44;mOi bom dia')
print('\033[1;35;43;mOi bom dia')
print('\033[30;42;mOi bom dia')
print('\033[mOi bom dia')
print('\033[7;30;mOi bom dia')

print('\033[1;31;43mOlá Mundo!\033[m')
print('\033[4;30;45mOlá Mundo!\033[m')
-------------------------------------------------------------------------------------------------------------------------------------
Funções
def imprime_nome(nome, sobrenome, idade):
    print(f'Olá, O seu nome é {nome} {nome1}, e voçê tem {idade}, anos.')
    

print(30 * '-')
nome = str(input('Qual é o seu nome: '))
nome1 = str(input('Qual é o seu sobrenome: '))
idade = int(input('Qual é a sua idade: '))
print(30 * '-')
imprime_nome(nome,nome1, idade)
-------------------------------------------------------------------------------------------------------------------------------------
Módulos
"""
Módulos - arquivos com extensão .py - ou seja - arquivos python
Pacotes - directórios contendo conjunto de módulos - pastas com vários arquivos python
"""

"""
from pacotes import principal, secundario

print(principal.soma(3, 2))

print(secundario.quadratica(5))
"""

from pacotes.sun_directorio import outro as módulos

print(módulos.cubica(3))

from pacotes.principal import soma
print(soma(13, 2))

from pacotes.sun_directorio.outro import cubica
print(cubica(2))
-------------------------------------------------------------------------------------------------------------------------------------
Módulo os

rint(os.path.exists('pacotes'))

#os.mknod('/users/Administrador/PycharmProjects/pythonExercicios/python_praticas/ex130.py')
#os.mknod('./pythonExercicios/pacote.txt')
#os.mkdir('python_praticas')
#os.mkdir('./python/documentos')
#os.remove('./python/documentos')
#print(os.path.exists('pacotes.txt'))
#os.mknod('olamundo.py')
#os.makedirs('nova_pasta')
#os.mknod('./nova_pasta/teste.py')
-----------------------------------------------------------------------------------------------------------------------------------
Trabalhar com arquivos de texto

arquivo = open('receitas.txt')
print(arquivo.read())
print(arquivo.closed)
arquivo.close()
print(arquivo.closed)

Lê linhas do texto

print(arquivo.readline())

Usando a função WITH
ler um arquivo de texto

with open('receitas.txt') as arquivo:
    print(arquivo.read())
    print(arquivo.closed)
print(arquivo.closed)

escrever num arquivo de texto

texto = """
ola, tudo bem
"""
with open('brigadeiro.txt', 'w' ) as arquivo:
    arquivo.write('texto')
    
ou dessa forma

arquivo = open('brigadeiro.txt', 'w')
arquivo.write('padaria')
arquivo.close()

Criar um arquivo PDF

import pydf

pdf = pydf.generate_pdf('<h1>ola mundo</h1><p>testando meu documento</p>n')

with open('outro_doc.pdf', 'wb') as arquivo:
    arquivo.write(pdf)
-----------------------------------------------------------------------------------------------------------------------------------
ENVIAR EMAIL

import smtplib
import ssl
import email.message

setor = 'B'
nome = 'Equipe do Finanveiro'

msg = email.message.Message()
msg['Subject'] = f'Planilha Financeira para Departamento: {setor}'

body = f"""
<p>Olá, {nome}</p>
<p>segue em anexo a planilha com os resultados desse mês</p>
<p>Atenciosamente,</p>
<p>Gerente ADM</p>
"""

msg['From'] = 'anferalta@gmail.com'
msg['To'] = 'anferalta@gmail.com'
password = '@nF1ralta24'
msg.add_header('Content-Type', 'text/html')
msg.set_payload(body)

context = ssl.create_default_context()
with smtplib.SMTP('smtp.gmail.com', 587) as conexao:
    conexao.ehlo()
    conexao.starttls(context=context)
    conexao.login(msg['From'], password)
    #conexao.login(msg['From'], password)
    conexao.sendmail(msg['From'], msg['To'], msg.as_string().encode('utf-8'))

SMTP SERVER DOMAIN NAME

Gmail  .................... smtp.gmail.com
Outlook ................... smtp-mail.outlook.com
Yahoo Mail ................ smtp.mail.yahoo.com    

PORTA: 587       

----------------------------------------------------------------------------------------------------------------------------------------------
Calcular quadrado

class Quadrado:
    
    def __init__(self, lado):
        self.lado = lado
        self.area = lado * lado

    
    def retorna_area(self): 
        print(self.area)


quadrado = Quadrado(2)
quadrado.retorna_area()
-----------------------------------------------------------------------------------------------------------------------------------------------        
analisar os seguintes exercícios:
ex010(conversão), ex012(desconto), ex013(calculo, salário), ex029(calculo velocidade), ex030(par, ímpar), ex031(calculo,distância)
ex033(maior,menor), ex034(calculo,salário), ex036(emprestimo,compra,casa) ex038(maior,maior), ex039(idade,militar), ex040(média,aluno)
ex041(atleta,categoria), ex043(calculo,IMC), ex044(condição,pagamento), ex045(jogo), ex046(contagem,fogos), ex047(contagem,pares)
ex049(tabuada), ex050(soma,pares), ex054(maior,menor,idade), ex055(peso,maior,menor), ex056(média,idade), ex057(validar,sexo)
ex058(jogo), ex059(operadores), ex067(tabuada), ex068(jogo), ex069(lê idade e sexo), ex070(compra productos), ex071(caixa electronico)
ex073(tabela classificativa), ex076(listagem de preços), ex077(vogais), ex078(maior, menor valor), ex079(valores duplicados) 
ex080(lista ordenada), ex081(lista decrescente), ex082(lista pares e ímpares), ex084(lista nome e peso), ex085(lista, pares,ímpares)
ex087(soma valores na lista), ex088(jogo), ex089(media alunos), ex





