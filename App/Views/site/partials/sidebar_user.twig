<div id="sidebar-wrapper"
     style="
     width: 240px;
     position: fixed;
     top: 0;
     left: 0;
     height: 100vh;
     z-index: 1030;
     ">

    <div id="sidebar"
         class="d-flex flex-column flex-shrink-0 p-3 bg-dark text-white"
         style="
         width: 240px;
         height: 100%;
         overflow-y: auto;
         ">

        <a href="{{ url('/dashboard') }}"
           class="d-flex align-items-center mb-4 text-white text-decoration-none">
            <span class="fs-4 fw-bold">O Meu Painel</span>
        </a>

        <div class="mb-4 ps-1">
            <span class="text-white-50">Utilizador:</span><br>
            <strong>{{ auth.user.nome }}</strong>
        </div>

        <ul class="nav nav-pills flex-column mb-auto">

            <li class="nav-item mb-2">
                <a href="{{ url('/dashboard') }}"
                   class="nav-link text-white d-flex align-items-center gap-2">
                    <i class="bi bi-speedometer2"></i>
                    Dashboard
                </a>
            </li>

            <li class="nav-item mb-2">
                <a href="{{ url('/documentos') }}"
                   class="nav-link text-white d-flex align-items-center gap-2">
                    <i class="bi bi-folder"></i>
                    Os Meus Documentos
                </a>
            </li>

            <li class="nav-item mb-2">
                <a href="{{ url('/documentos/criar') }}"
                   class="nav-link text-white d-flex align-items-center gap-2">
                    <i class="bi bi-plus-circle"></i>
                    Criar Documento
                </a>
            </li>

            {# LOGOUT SEGURO — VISUALMENTE IDÊNTICO AO LINK ORIGINAL #}
            <li class="nav-item mt-4">

                <form action="/logout" method="POST" class="m-0 p-0">
                    <input type="hidden" name="_csrf" value="{{ _csrf_global }}">

                    <button type="submit"
                            class="nav-link text-danger d-flex align-items-center gap-2 border-0 bg-transparent w-100 text-start"
                            style="cursor:pointer;">
                        <i class="bi bi-box-arrow-right"></i>
                        Sair
                    </button>
                </form>

            </li>

        </ul>

    </div>
</div>
