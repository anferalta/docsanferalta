{% extends "@admin/layout.twig" %}

{% block content %}
<h3>Editar Permissão</h3>

<form method="POST">

    {{ csrf_field() }}

    <div class="mb-3">
        <label class="form-label">Código</label>
        <input type="text" name="codigo" class="form-control" value="{{ permissao.codigo }}" required>
    </div>

    <div class="mb-3">
        <label class="form-label">Descrição</label>
        <input type="text" name="descricao" class="form-control" value="{{ permissao.descricao }}" required>
    </div>

    <button class="btn btn-primary">Guardar</button>
    <a href="/admin/permissoes" class="btn btn-secondary">Cancelar</a>
</form>
{% endblock %}